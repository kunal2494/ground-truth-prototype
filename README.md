# Ground Truth Prototype

Static demo for the Ground Truth answer and expert contribution experience.

Open `index.html` locally, or publish the repository with GitHub Pages from the `main` branch root.
